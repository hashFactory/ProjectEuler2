package Support;

import jdk.jfr.Description;
import jdk.jfr.Name;

public class IntegerMethods
{

    @Name("isPerfect")
    @Description("Returns int with -1 if deficient, 0 if perfect, and 1 if abundant")
    public static int isPerfect(int input)
    {
        // TODO: Make this if required
        return 0;
    }

}
